// Generated by CoffeeScript 1.4.0
(function() {
  var Article, Storage, article, hideSport, view, _i, _len, _ref,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Article = (function(_super) {

    __extends(Article, _super);

    function Article() {
      this.render = __bind(this.render, this);

      this.key = __bind(this.key, this);

      this.toggleHideState = __bind(this.toggleHideState, this);

      this.initialize = __bind(this.initialize, this);
      return Article.__super__.constructor.apply(this, arguments);
    }

    Article.prototype.initialize = function() {
      var hider, wrapper;
      wrapper = $('<div class="angel-wrapper"></div>');
      hider = $('<input class="angel-hider" type="checkbox"/>');
      this.$el.wrap(wrapper);
      this.$el.before(hider);
      if (!!Storage.get(this.key())) {
        hider.click();
      }
      hider.click(this.toggleHideState);
      return this.render();
    };

    Article.prototype.toggleHideState = function() {
      Storage.set(this.key(), !Storage.get(this.key()));
      console.log("toggleHideState " + (this.key()) + " -> " + (Storage.get(this.key())));
      return this.render();
    };

    Article.prototype.key = function() {
      return "angel-" + this.$('a')[0].href;
    };

    Article.prototype.render = function() {
      return this.$el.toggle(!Storage.get(this.key()));
    };

    return Article;

  })(Backbone.View);

  Storage = {
    set: function(key, value) {
      try {
        return localStorage.setItem(key, value);
      } catch (error) {
        if (error.name === 'QUOTA_EXCEEDED_ERR') {
          localStorage.clear();
          return localStorage.setItem(key, value);
        } else {
          throw error;
        }
      }
    },
    get: function(key) {
      var value;
      value = localStorage.getItem(key);
      if (value === 'true') {
        return true;
      } else if (value === 'false') {
        return false;
      } else {
        return value;
      }
    },
    remove: function(key) {
      return localStorage.removeItem(key);
    }
  };

  _ref = $("li.inline-pic, li.pixie, li.mugshot");
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    article = _ref[_i];
    view = new Article({
      el: article
    });
  }

  hideSport = true;

  if (hideSport) {
    $("#sport-nwf-picks").hide();
  }

}).call(this);
